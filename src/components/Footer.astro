---
import { menu } from "../../db";
---

<footer>
  <div class="footer">
   
    <ul class="footer-menu">
      {menu.map((menuItem, i) => (
        <li>
          <a
            href={menuItem.path}
            target={menuItem.name === "Site Map" ? "_blank" : ""}
          >
            {menuItem.name}
          </a>
        </li>
      ))}
    </ul>
    <p>
      {`© Health Quiz ${new Date().getFullYear()}`}{" "}
    </p>
  </div>
</footer>

